{"schema":"mst-knowledge-proposals/v1","proposals":["Fresh install must verify pointer-selected manifest bytes before using any asset paths. Subsequent upgrades may use exact image-bound local evidence, not labels alone.","Program-only updates use one fixed verified foundation, never an indefinitely growing image-parent chain. Cold-layer incompatibility requires an explicit full install.","Backups not implemented must fail, and disposable smoke must never reset an existing instance by name."]}
